pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,jsx,ts,tsx,vue}"
      run: yarn lint --fix {staged_files}
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: yarn commitlint --edit {1}